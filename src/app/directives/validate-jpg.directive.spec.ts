import { ValidateJPGDirective } from './validate-jpg.directive';

describe('ValidateJPGDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateJPGDirective();
    expect(directive).toBeTruthy();
  });
});
